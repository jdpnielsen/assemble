import { TemplateConfig } from '../helper';
import { <%= it.componentName %>Data, <%= it.type %>DataLoader } from './<%= it.name %>.query';
import { <%= it.componentName %> } from './<%= it.name %>.template';

export const <%= it.type %>Config: TemplateConfig<'<%= it.type %>', <%= it.componentName %>Data> = {
	name: '<%= it.type %>',
	dataLoader: <%= it.type %>DataLoader,
	Component: <%= it.componentName %>,
};
